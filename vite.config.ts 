// --- ./vite.config.ts ---
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    // Configuración útil para desarrollo local (no afecta producción)
    host: true,
    strictPort: true,
    port: 5173
  },
  build: {
    // Optimizaciones para producción
    target: 'esnext',
    outDir: 'dist',
    sourcemap: false
  }
})